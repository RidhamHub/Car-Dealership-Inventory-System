# Base URL of the backend API.
# Leave as /api in development — Vite proxies it to http://localhost:5000.
# In production set it to your deployed API, e.g. https://your-api.onrender.com/api
VITE_API_URL=/api
